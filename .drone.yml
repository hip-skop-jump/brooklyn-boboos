kind: pipeline
type: docker
name: test

platform:
  os: linux
  arch: amd64

steps:
  - name: test
    image: node:14
    commands:
      - npm i
      - npm run eslint
      - npm run build:prod

trigger:
  branch:
    - master
  event:
    - push
    - pull_request
